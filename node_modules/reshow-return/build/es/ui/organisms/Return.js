import _objectSpread from "reshow-runtime/es/helpers/objectSpread2";
import _objectWithoutProperties from "reshow-runtime/es/helpers/objectWithoutProperties";
var _excluded = ["children"];
import React from "react";
import build from "reshow-build";
import { useConnect } from "reshow-flux";
import connectOptions from "../../src/connectOptions";

var getReturn = function getReturn(_ref) {
  var useConnect = _ref.useConnect,
      cleanProps = _ref.cleanProps,
      _ref$displayName = _ref.displayName,
      displayName = _ref$displayName === void 0 ? "Return" : _ref$displayName;

  var Return = function Return(props) {
    var children = props.children,
        otherProps = _objectWithoutProperties(props, _excluded);

    var state = useConnect(props);
    var result = build(children)(_objectSpread(_objectSpread({}, connectOptions.reset(otherProps, cleanProps)), state));
    return result;
  };

  Return.displayName = displayName;
  return Return;
};

var Return = getReturn({
  useConnect: useConnect(connectOptions)
});
export default Return;
export { getReturn };