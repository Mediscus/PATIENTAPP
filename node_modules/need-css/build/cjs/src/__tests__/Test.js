"use strict";

var _interopRequireDefault = require("reshow-runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _reshowUnitDom = require("reshow-unit-dom");

var _index = _interopRequireDefault(require("../index"));

describe("Test need-css", function () {
  beforeEach(function () {
    (0, _reshowUnitDom.jsdom)(null, {
      runScripts: "dangerously",
      resources: "usable"
    });
  });
  it("basic testt", function (done) {
    (0, _index["default"])(["prismjs"]);
    setTimeout(function () {
      (0, _chai.expect)(document.getElementsByTagName("link").length).to.equal(1);
      done();
    }, 500);
  });
});