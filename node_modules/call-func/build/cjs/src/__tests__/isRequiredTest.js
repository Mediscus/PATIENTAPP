"use strict";

var _chai = require("chai");

var _index = require("../index.js");

describe("Test isRequired", function () {
  it("basic test", function () {
    var a = function a() {
      var foo = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : (0, _index.isRequired)("foo");
    };

    (0, _chai.expect)(a).to["throw"]("foo");

    var b = function b() {
      return a("");
    };

    (0, _chai.expect)(b).to.not["throw"]();
  });
});