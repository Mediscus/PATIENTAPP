import { Axis, AxisOptions, GridDimensions, Series } from '../types';
export default function buildAxisLinear<TDatum>(userOptions: AxisOptions<TDatum>, series: Series<TDatum>[], gridDimensions: GridDimensions, width: number, height: number): Axis<TDatum>;
