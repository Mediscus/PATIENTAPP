import { Axis, Series } from '../types';
export default function AreaComponent<TDatum>({ primaryAxis, secondaryAxis, series: allSeries, }: {
    primaryAxis: Axis<TDatum>;
    secondaryAxis: Axis<TDatum>;
    series: Series<TDatum>[];
}): JSX.Element;
