export declare type HasBoundingClientRect = {
    getBoundingClientRect: () => DOMRect;
};
export default function useRect(node: HasBoundingClientRect | null | undefined, options?: {
    enabled?: boolean;
    initialWidth?: number;
    initialHeight?: number;
}): DOMRect;
