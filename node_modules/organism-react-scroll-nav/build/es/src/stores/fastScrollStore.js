import _assertThisInitialized from "reshow-runtime/es/helpers/assertThisInitialized";
import _inheritsLoose from "reshow-runtime/es/helpers/inheritsLoose";
import _defineProperty from "reshow-runtime/es/helpers/defineProperty";
import { ImmutableStore } from "reshow-flux";
import { Scroller } from "./scrollStore";

var FastScroller = /*#__PURE__*/function (_Scroller) {
  _inheritsLoose(FastScroller, _Scroller);

  function FastScroller() {
    var _this;

    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    _this = _Scroller.call.apply(_Scroller, [this].concat(args)) || this;

    _defineProperty(_assertThisInitialized(_this), "storeName", "fastScroll");

    return _this;
  }

  var _proto = FastScroller.prototype;

  _proto.runScrollMonitor = function runScrollMonitor(e) {
    this.triggerScroll(e.target);
  };

  return FastScroller;
}(Scroller);

var oFastScroller = new FastScroller();

var _ImmutableStore = ImmutableStore(oFastScroller.reduce.bind(oFastScroller), oFastScroller.getInitialState.bind(oFastScroller)),
    store = _ImmutableStore[0],
    fastScrollDispatch = _ImmutableStore[1];

store.scroller = oFastScroller;
oFastScroller.dispatch = fastScrollDispatch;
oFastScroller.store = store;
export default store;