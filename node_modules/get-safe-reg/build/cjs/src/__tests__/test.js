"use strict";

var _interopRequireDefault = require("reshow-runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _index = _interopRequireDefault(require("../index.js"));

describe("test get safe reg", function () {
  var n;
  it("test get safe reg", function () {
    n = (0, _index["default"])("/[|\\{}()[]^$+*?.]/g");
    (0, _chai.expect)(n).to.equal("/\\[\\|\\\\\\{\\}\\(\\)\\[\\]\\^\\$\\+\\*\\?\\.\\]/g");
  });
  it("test reg is valid", function () {
    var patt = new RegExp(n);
    var res = patt.test("/[|\\{}()[]^$+*?.]/g");
    (0, _chai.expect)(res).to.be["true"];
  });
  it("test with undefined", function () {
    var acture = (0, _index["default"])();
    (0, _chai.expect)(acture).to.equal("");
  });
});