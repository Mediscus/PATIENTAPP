"use strict";

var _interopRequireDefault = require("reshow-runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _easeInBack = _interopRequireDefault(require("../easeInBack"));

describe('Test easeInBack', function () {
  it("basic test", function () {
    var start = 0;
    var to = 100;
    var duration = 5;
    var actual1 = (0, _easeInBack["default"])(1, start, to, duration).toFixed(2);
    var actual2 = (0, _easeInBack["default"])(2, start, to, duration).toFixed(2);
    var actual3 = (0, _easeInBack["default"])(3, start, to, duration).toFixed(2);
    var actual4 = (0, _easeInBack["default"])(4, start, to, duration).toFixed(2);
    var actual5 = (0, _easeInBack["default"])(5, start, to, duration).toFixed(2);
    (0, _chai.expect)({
      actual1: "-4.65",
      actual2: "-9.94",
      actual3: "-2.90",
      actual4: "29.42",
      actual5: "100.00"
    }).to.deep.equal({
      actual1: actual1,
      actual2: actual2,
      actual3: actual3,
      actual4: actual4,
      actual5: actual5
    });
  });
});