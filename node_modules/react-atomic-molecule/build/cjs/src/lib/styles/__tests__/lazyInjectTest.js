"use strict";

var _interopRequireDefault = require("reshow-runtime/helpers/interopRequireDefault");

var _chai = require("chai");

var _lazyInject = _interopRequireDefault(require("../lazyInject"));

var _stylesToCSS = _interopRequireDefault(require("../stylesToCSS"));

var _store = _interopRequireDefault(require("../store"));

describe("Test lazyInject", function () {
  beforeEach(function () {
    _store["default"].counter = 0;
    _store["default"].lastCompiled = null;
  });
  it("test keyframe", function () {
    var injects;
    var InjectStyles = {
      fakeKeyframe: [[{
        transform: ["rotateZ(0deg)"]
      }, {
        transform: ["rotateZ(360deg)"]
      }], ["@keyframes spin", "0%", "100%"]]
    };
    injects = (0, _lazyInject["default"])(injects, InjectStyles);
    (0, _chai.expect)(JSON.stringify(_store["default"].lastCompiled.cssArr)).to.have.string("@keyframes spin");
  });
});